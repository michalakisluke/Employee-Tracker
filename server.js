const inquirer = require('inquirer');
const cTable = require('console.table');
const team = [];

function init() {
    // Prompt for team manager info
    inquirer.prompt([
        
    ]).then((answers) => {
    });
}

init();